# Store 1.0 (4): permission readability and device identity

- Permission instructions are 14 points instead of the Settings caption size (11 points). The primary guide is three steps, with optional import/troubleshooting details in a disclosure row. Metadata-cleanup copy is shorter.
- Import setup explains same-name drives and profile naming. Device selection still uses the filesystem volume UUID and saved scoped media bookmark; DJI/DCIM folder names never select an automatic source on their own. No import, cleanup or eject behavior changed.
- Release archive built for arm64 and x86_64 with preserved Xcode 26.6 RC2. Apple build `2845f6aa-95ee-42db-b839-e2363f7df6e8` is VALID / APP_STORE_ELIGIBLE.
- Native preview of the archive executable, using an isolated bundle/container, verified Settings text, collapsed/expanded details and first-launch Help. Actual Settings capture is used in the permission artwork; user-supplied menu screenshot has surrounding desktop distractions removed with imagegen. All menu labels/order were visually compared with the original. Original image and cleanup prompt are retained.
- Signed sandbox identity probe compiled with production ImportVolumes, ScopedFolder and MediaImportEngine. Two generated HFS+ images have the same volume label and DCIM/DJI_001 folder. After enrollment, app relaunch and reversing connection order, the enrolled card mounted at `/Volumes/EE Identity Fixture 1`. UUID matching and the saved bookmark selected it, copied the expected bytes through the real engine, retained its original and excluded the decoy. First harness run used hidden/nobrowse images, intentionally excluded by mounted-volume discovery; normal mounts passed. Both images detached afterward. This checks shared selection/bookmark/engine components, not a new physical DJI recording test.
- No production profiles or real card files were changed. Full prior engine/eject parity verification remains applicable; this release only changes UI copy/type size and build number.

Evidence: `release-evidence/store-readability-release/` at the canonical EasyEject workspace. `Tests/StorePrototype/VolumeIdentityProbe.swift` is the manual GUI harness; compile with `-D APP_STORE` and the three production files above, sign a standalone app with Store/Store.entitlements and a unique test bundle ID. Use generated fixtures only.
